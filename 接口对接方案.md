# 云宽带小程序接口对接方案

## 📋 目录
1. [系统功能概览](#系统功能概览)
2. [核心功能接口对接](#核心功能接口对接)
3. [不需要对接的接口](#不需要对接的接口)
4. [接口对接优先级](#接口对接优先级)

---

## 系统功能概览

根据当前小程序已实现的功能页面，系统包含以下核心模块：

### ✅ 已实现的页面功能
1. **用户认证** - 登录页面 (pages/login)
2. **首页** - 功能导航 (pages/home)
3. **个人中心** - 用户信息 (pages/my)
4. **设备绑定** - 绑定设备码 (pages/bind-device-code)
5. **套餐订购** - 套餐选择与订购 (pages/package-order)
6. **账单管理** - 我的账单 (pages/my-bill)
7. **预充值** - 余额充值 (pages/pre-recharge)
8. **开票申请** - 发票管理 (pages/invoice)
9. **客服服务** - 在线客服 (pages/customer-service)
10. **业务申请** - 各类业务办理 (pages/business-application)
11. **业务退订** - 退订服务 (pages/business-cancellation)
12. **变更过户** - 账户过户 (pages/change-transfer)
13. **自助续费** - 套餐续费 (pages/self-renewal)
14. **电子协议** - 协议查看 (pages/electronic-agreement)
15. **代缴代扣** - 支付管理 (pages/payment-collection)
16. **产品查询** - 产品信息 (pages/product-query)
17. **举报投诉** - 投诉管理 (pages/complaint)
18. **服务评价** - 评价系统 (pages/service-evaluation)

---

## 核心功能接口对接

### 🔐 1. 用户认证模块 (登录页面)

#### 必须对接的接口：

**1.1 微信登录流程**
```
POST /api/getOpenidByCode
- 功能：通过微信code获取openid
- 用途：用户首次登录时获取微信身份标识
- 优先级：⭐⭐⭐⭐⭐ (最高)
```

**1.2 生成用户Token**
```
POST /api/generateTokenByOpenid
- 功能：通过openid生成JWT token
- 用途：用户登录后获取访问凭证
- 优先级：⭐⭐⭐⭐⭐ (最高)
```

**1.3 创建用户**
```
POST /api/createUser
- 功能：创建新用户并生成token
- 用途：新用户首次登录时创建账户
- 优先级：⭐⭐⭐⭐⭐ (最高)
```

**1.4 获取用户信息**
```
GET /api/getUserInfo
- 功能：获取当前登录用户信息
- 用途：个人中心展示用户资料
- 优先级：⭐⭐⭐⭐⭐ (最高)
```

---

### 📱 2. 设备绑定模块 (bind-device-code)

#### 必须对接的接口：

**2.1 绑定设备到用户**
```
POST /api/bindDevice
- 功能：将设备绑定到当前用户
- 用途：用户输入设备码后绑定设备
- 优先级：⭐⭐⭐⭐⭐ (最高)
```

**2.2 获取用户绑定的设备列表**
```
GET /api/getUserDevices
- 功能：获取当前用户绑定的所有设备
- 用途：个人中心展示已绑定设备
- 优先级：⭐⭐⭐⭐
```

**2.3 解绑设备**
```
POST /api/unbindDevice
- 功能：解除用户与设备的绑定
- 用途：用户需要更换设备时解绑
- 优先级：⭐⭐⭐⭐
```

**2.4 根据设备码查询客户信息**
```
GET /api/getCustomerByDeviceCode/:device_code
- 功能：根据设备码查询绑定的客户信息
- 用途：验证设备码是否有效
- 优先级：⭐⭐⭐⭐
```

---

### 📦 3. 套餐管理模块 (package-order)

#### 必须对接的接口：

**3.1 获取套餐列表**
```
GET /api/packages/getPackagesList
- 功能：获取所有可用套餐
- 用途：套餐订购页面展示套餐选项
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：page, pageSize, status (筛选激活状态的套餐)
```

**3.2 获取套餐详情**
```
GET /api/packages/getPackageDetail/:id
- 功能：获取指定套餐的详细信息
- 用途：查看套餐详细特性和价格
- 优先级：⭐⭐⭐⭐
```

**3.3 创建订单**
```
POST /api/orders
- 功能：创建套餐订购订单
- 用途：用户选择套餐后创建订单
- 优先级：⭐⭐⭐⭐⭐ (最高)
```

---

### 💰 4. 账单管理模块 (my-bill)

#### 必须对接的接口：

**4.1 获取账单列表**
```
GET /api/customerBill/getCustomerBillList
- 功能：获取用户的账单列表
- 用途：我的账单页面展示账单记录
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：page, pageSize, customerId, status
```

**4.2 获取账单详情**
```
GET /api/customerBill/getCustomerBillDetail/:id
- 功能：获取账单详细信息
- 用途：查看账单明细
- 优先级：⭐⭐⭐⭐
```

---

### 💳 5. 预充值模块 (pre-recharge)

#### 必须对接的接口：

**5.1 小程序支付 - 创建支付订单**
```
POST /api/miniprogram/pay
- 功能：创建小程序支付订单
- 用途：用户选择充值金额后发起支付
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：amount, description, openid
```

**5.2 查询支付状态（增强版）**
```
GET /api/payments/status/enhanced
- 功能：查询支付状态（支持实时查询微信支付状态）
- 用途：轮询查询支付是否完成
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：order_no 或 order_id
```

**5.3 取消支付**
```
POST /api/payments/cancel
- 功能：取消未完成的支付
- 用途：用户取消充值操作
- 优先级：⭐⭐⭐
```

---

### 🧾 6. 开票管理模块 (invoice)

#### 必须对接的接口：

**6.1 获取发票列表**
```
GET /api/v1/invoices/getInvoiceList
- 功能：获取可开票的订单列表（已支付且已审核）
- 用途：开票页面展示可开票订单
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：page, limit, customer_name, device_no, invoice_status
```

**6.2 创建或更新开票信息**
```
POST /api/v1/invoices/invoice-info/createOrUpdateInvoiceInfo
- 功能：保存用户的开票信息
- 用途：用户填写发票抬头等信息
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：customer_name, invoice_title, invoice_type, taxpayer_id, address, phone, bank_name, bank_account
```

**6.3 根据设备号获取开票信息**
```
GET /api/v1/invoices/invoice-info/device/:device_no
- 功能：获取已保存的开票信息
- 用途：自动填充用户之前保存的开票信息
- 优先级：⭐⭐⭐⭐
```

**6.4 为订单创建发票**
```
POST /api/v1/invoices/create/:id
- 功能：为指定订单创建发票
- 用途：用户提交开票申请
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：invoice_title, taxpayer_id, address, phone, bank_name, bank_account
```

---

### 📝 7. 订单管理模块 (通用)

#### 必须对接的接口：

**7.1 查询订单列表**
```
GET /api/orders
- 功能：查询用户的订单列表
- 用途：查看历史订单、订单状态
- 优先级：⭐⭐⭐⭐⭐ (最高)
- 参数：page, pageSize, device_no, customer_name, review_status, payment_status
```

**7.2 获取订单详情**
```
GET /api/orders/:id
- 功能：获取订单详细信息
- 用途：查看订单详情
- 优先级：⭐⭐⭐⭐
```

---

### 🔧 8. 工具类接口

#### 建议对接的接口：

**8.1 获取省份列表**
```
GET /api/v1/tools/provinces
- 功能：获取中国所有省份列表
- 用途：地址选择器（开票地址、业务申请地址等）
- 优先级：⭐⭐⭐
```

**8.2 获取城市列表**
```
GET /api/v1/tools/provinces/:provinceId/cities
- 功能：根据省份获取城市列表
- 用途：地址选择器
- 优先级：⭐⭐⭐
```

**8.3 获取Banner列表**
```
GET /api/v1/tools/getBannersList
- 功能：获取首页轮播图
- 用途：首页展示Banner广告
- 优先级：⭐⭐⭐
```

**8.4 获取推文列表**
```
GET /api/v1/tools/getArticlesList
- 功能：获取公众号推文列表
- 用途：首页展示资讯内容
- 优先级：⭐⭐
```

**8.5 获取推文详情**
```
GET /api/v1/tools/getArticleDetail/:id
- 功能：获取推文详细内容
- 用途：查看资讯详情
- 优先级：⭐⭐
```

---

## 不需要对接的接口

以下接口属于**管理后台功能**，小程序端**不需要对接**：

### ❌ 管理员认证相关
- `POST /api/admin/login` - 管理员登录
- `POST /api/admin/logout` - 管理员登出
- `GET /api/admin/getWebUserInfo` - 获取管理员信息
- `POST /api/admin/change-password` - 修改管理员密码
- `POST /api/admin/create-account` - 创建管理员账户
- `GET /api/admin/getWebUserList` - 获取管理员列表
- `POST /api/admin/delete-account` - 删除管理员账户
- `PUT /api/admin/updateWebUser` - 更新管理员信息

### ❌ 设备管理相关（管理员功能）
- `POST /api/createDevice` - 创建设备
- `GET /api/getDevicesList` - 获取设备列表
- `GET /api/getDeviceDetail/:id` - 获取设备详情
- `PUT /api/updateDevice/:id` - 更新设备信息
- `DELETE /api/deleteDevice/:id` - 删除设备

### ❌ 客户管理相关（管理员功能）
- `POST /api/createCustomer` - 创建客户
- `GET /api/getCustomerLists` - 获取客户列表
- `GET /api/getCustomerDetail/:id` - 获取客户详情
- `PUT /api/updateCustomer/:id` - 更新客户信息
- `DELETE /api/deleteCustomer/:id` - 删除客户

### ❌ 订单管理相关（管理员功能）
- `PUT /api/orders/payment-status` - 修改订单支付状态
- `DELETE /api/orders/:id` - 删除订单
- `PUT /api/orders/:id/review` - 更新订单审核状态

### ❌ 客户设备绑定管理（管理员功能）
- `POST /api/createBinding` - 创建绑定关系
- `GET /api/getBindingsList` - 获取绑定列表
- `GET /api/getBindingsByCustomer/:customer_id` - 根据客户ID获取绑定
- `GET /api/checkDeviceBinding/:device_id` - 检查设备绑定状态
- `GET /api/getBindingDetail/:id` - 获取绑定详情
- `PUT /api/updateBinding/:id` - 更新绑定信息
- `DELETE /api/deleteBinding/:id` - 解除绑定

### ❌ 文件管理相关（管理员功能）
- `POST /api/upload` - 上传文件（管理员）
- `DELETE /api/:fileId` - 删除文件（管理员）
- `GET /api/list` - 获取文件列表

### ❌ 邮件服务相关（管理员功能）
- `POST /api/email/send-attachment` - 发送邮件
- `GET /api/email/test-config` - 测试邮件配置

### ❌ 套餐管理相关（管理员功能）
- `POST /api/packages/addPackage` - 新增套餐
- `PUT /api/packages/updatePackage/:id` - 更新套餐
- `DELETE /api/packages/deletePackage/:id` - 删除套餐

### ❌ 账单管理相关（管理员功能）
- `POST /api/customerBill/initTable` - 初始化账单表
- `PUT /api/customerBill/updateBillStatus/:id` - 更新账单状态
- `GET /api/customerBill/exportBillPDF/:id` - 导出账单PDF
- `POST /api/customerBill/sendBillEmail/:id` - 发送账单邮件

### ❌ 发票管理相关（管理员功能）
- `POST /api/v1/invoices/initialize` - 初始化发票表
- `GET /api/v1/invoices/invoice-info/list` - 获取开票信息列表
- `DELETE /api/v1/invoices/invoice-info/:id` - 删除开票信息

### ❌ Banner和推文管理（管理员功能）
- `POST /api/v1/tools/createBanner` - 创建Banner
- `DELETE /api/v1/tools/deleteBanner/:id` - 删除Banner
- `PUT /api/v1/tools/updateBannerStatus/:id/status` - 更新Banner状态
- `POST /api/v1/tools/createArticle` - 创建推文
- `PUT /api/v1/tools/updateArticle/:id` - 更新推文
- `DELETE /api/v1/tools/deleteArticle/:id` - 删除推文
- `PUT /api/v1/tools/updateArticleStatus/:id` - 更新推文状态

### ❌ 统计和日志相关（管理员功能）
- `GET /api/v1/tools/stats/customers` - 获取客户统计信息
- `GET /api/v1/tools/operation-logs` - 获取系统操作日志

### ❌ 管理员支付相关
- `POST /api/admin/payments/web/create` - 管理员创建网页支付
- `POST /api/admin/miniprogram/pay` - 管理员创建小程序支付
- `GET /api/admin/payments/status` - 管理员查询支付状态
- `POST /api/admin/payments/cancel` - 管理员取消支付

### ❌ 支付回调接口（由支付平台调用）
- `POST /api/payments/wechat-notify` - 微信支付回调
- `POST /api/payments/alipay-notify` - 支付宝异步回调
- `GET /api/payments/alipay-return` - 支付宝同步回调

### ❌ 其他不需要的接口
- `GET /api/getAllWxUsers` - 获取所有小程序用户（管理员功能）
- `GET /api/download/:fileId` - 文件下载（如需要可对接）

---

## 接口对接优先级

### 🔴 第一优先级（核心功能，必须立即对接）

1. **用户认证流程**
   - `POST /api/getOpenidByCode`
   - `POST /api/generateTokenByOpenid`
   - `POST /api/createUser`
   - `GET /api/getUserInfo`

2. **设备绑定**
   - `POST /api/bindDevice`
   - `GET /api/getUserDevices`

3. **套餐订购**
   - `GET /api/packages/getPackagesList`
   - `POST /api/orders`

4. **账单查询**
   - `GET /api/customerBill/getCustomerBillList`

5. **支付功能**
   - `POST /api/miniprogram/pay`
   - `GET /api/payments/status/enhanced`

### 🟡 第二优先级（重要功能，建议尽快对接）

1. **订单管理**
   - `GET /api/orders`
   - `GET /api/orders/:id`

2. **开票功能**
   - `GET /api/v1/invoices/getInvoiceList`
   - `POST /api/v1/invoices/invoice-info/createOrUpdateInvoiceInfo`
   - `POST /api/v1/invoices/create/:id`

3. **设备管理**
   - `POST /api/unbindDevice`
   - `GET /api/getCustomerByDeviceCode/:device_code`

4. **套餐详情**
   - `GET /api/packages/getPackageDetail/:id`

### 🟢 第三优先级（辅助功能，可后续对接）

1. **工具类接口**
   - `GET /api/v1/tools/provinces`
   - `GET /api/v1/tools/provinces/:provinceId/cities`
   - `GET /api/v1/tools/getBannersList`

2. **内容展示**
   - `GET /api/v1/tools/getArticlesList`
   - `GET /api/v1/tools/getArticleDetail/:id`

3. **账单详情**
   - `GET /api/customerBill/getCustomerBillDetail/:id`

4. **开票信息查询**
   - `GET /api/v1/invoices/invoice-info/device/:device_no`
   - `GET /api/v1/invoices/invoice-info/customer/:customer_id`

5. **支付取消**
   - `POST /api/payments/cancel`

---

## 接口对接总结

### 📊 统计数据

- **API接口文档总接口数**: 约 120+ 个
- **需要对接的接口数**: 约 30-35 个
- **不需要对接的接口数**: 约 85-90 个（主要是管理后台功能）
- **对接比例**: 约 25-30%

### 🎯 对接建议

1. **分阶段实施**
   - 第一阶段：对接第一优先级接口（用户认证、设备绑定、套餐订购、支付）
   - 第二阶段：对接第二优先级接口（订单管理、开票功能）
   - 第三阶段：对接第三优先级接口（工具类、内容展示）

2. **接口封装**
   - 建议创建统一的 API 请求封装层（如 `utils/api.js`）
   - 统一处理 token 认证、错误处理、loading 状态

3. **数据缓存**
   - 套餐列表、省市数据等可以缓存到本地存储
   - 减少不必要的网络请求

4. **错误处理**
   - 统一处理网络错误、token 过期等情况
   - 提供友好的错误提示

---

## 📝 下一步行动

1. ✅ 创建 API 请求封装工具 (`utils/api.js`)
2. ✅ 配置 API 基础地址和请求拦截器
3. ✅ 实现第一优先级接口对接
4. ✅ 测试核心功能流程
5. ✅ 逐步完善其他功能接口

---

**文档版本**: v1.0
**创建日期**: 2024-12-15
**最后更新**: 2024-12-15


