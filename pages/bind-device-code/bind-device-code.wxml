<!--pages/bind-device-code/bind-device-code.wxml-->
<view class="bind-device-page">
  <!-- 顶部标题区域 -->
  <view class="header-section">
    <text class="title">绑定您的设备</text>
    <text class="subtitle">请输入您的设备绑定码</text>
  </view>

  <!-- 主内容区域 -->
  <view class="content-wrapper">
    <view class="bind-card">
      <!-- 输入表单 -->
      <view class="input-section">
        <view class="input-wrapper">
          <text class="input-icon">📱</text>
          <input
            type="text"
            value="{{deviceCode}}"
            bindinput="onInputChange"
            class="custom-input"
            placeholder="请输入设备绑定码"
            placeholder-style="color: #9ca3af"
          />
        </view>
        
        <button 
          class="submit-button {{isLoading ? 'loading' : ''}}" 
          bindtap="onManualSubmit"
          disabled="{{isLoading}}"
        >
          <text wx:if="{{!isLoading}}" class="button-icon">✓</text>
          <text wx:if="{{isLoading}}" class="loading-icon">⟳</text>
          {{isLoading ? '绑定中...' : '确认绑定'}}
        </button>
      </view>
      
      <!-- 跳过按钮 -->
      <view class="skip-section">
        <button class="skip-button" bindtap="skipBinding">
          暂时跳过
        </button>
      </view>

      <!-- 帮助信息 -->
      <view class="help-section">
        <text class="help-text" bindtap="showHelp">
          找不到绑定码？点击查看帮助
        </text>
      </view>
    </view>
  </view>
</view>

